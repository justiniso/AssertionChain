#!/usr/bin/env python

from setuptools import setup

setup(
    name='assertionchain',
    version='0.1.0',
    author='Justin Iso',
    install_requires=[],
    tests_require=[],
    test_suite='tests',
    setup_requires=[],
    description='',
    url='',
)